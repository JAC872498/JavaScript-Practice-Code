var str=prompt("XD");
var newStr="";
for(var x=0;x<str.length;x++){
	if(x%2==0){
		if(str[x]*2>9){
			newStr=newStr+((str[x]*2)-9);
		}else{
			newStr=newStr+(str[x]*2);
		}
	}else{
		newStr=newStr+str[x];
	}
}
console.log(newStr);
